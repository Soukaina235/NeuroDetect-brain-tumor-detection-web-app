// we are using that because we want to use our locale storage to store
// the access token and the refresh token in the browser and we want to
// have a key that we can use to access in my local storage
export const ACCESS_TOKEN = "access";
export const REFRESH_TOKEN = "refresh";
